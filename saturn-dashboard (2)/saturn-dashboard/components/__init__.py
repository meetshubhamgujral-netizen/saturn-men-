"""Reusable presentational UI components for the Saturn dashboard.

This file intentionally contains a docstring (not empty) so it is never dropped
by GitHub's drag-and-drop folder uploader, which silently skips 0-byte files.
"""
