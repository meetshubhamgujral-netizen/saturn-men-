"""Saturn dashboard computation core (Streamlit-free, testable).

Marking this directory as a proper package so imports like ``from utils import
analytics`` resolve reliably on every platform, including Streamlit Cloud.
"""
